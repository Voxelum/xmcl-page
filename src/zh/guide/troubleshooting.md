# 常见安装与启动故障排查

如果您在安装 Minecraft 游戏、模组加载器（Forge/Fabric/NeoForge/Quilt）、模组、模组包或光影时遇到问题，或者游戏无法正常启动，本指南将帮助您逐步排查并解决这些故障。

---

## 🌐 1. 下载失败或进度卡住 (网络连接问题)

### 故障表现
* 下载 Minecraft、游戏资源、依赖库或 Forge/Fabric 时进度一直卡在 `0%`。
* 启动器弹出超时或连接失败错误（`CONNECTION_TIMED_OUT`、`NAME_NOT_RESOLVED`、`HTTP_STATUS 504`）。

### 解决方案

:::tip 尝试切换下载源（镜像站）
如果官方 Mojang 或 Forge 服务器连接缓慢或被您的网络运营商拦截，您可以一键切换至国内的高速镜像源：
1. 点击左侧边栏底部的 **设置**（齿轮图标）。
2. 向下滚动至 **网络设置** 区域。
3. 找到 **下载源 / 镜像**（Download Source / Mirror）设置项。
4. 将其从 **默认**（Default）切换为 **BMCLAPI** 或 **MCBBS**（这两个是国内非常稳定且高速的官方同步镜像源）。
:::

:::info 配置网络代理
如果您的网络环境需要通过代理访问外部网络服务，您可以在启动器中配置代理：
1. 在 **设置** -> **网络设置** 中，找到代理配置项。
2. 配置您的 SOCKS5 或 HTTP 代理服务器地址。
3. 点击测试以确保代理可用。
:::

---

## 📦 2. 模组或模组包部分文件下载失败 (CurseForge 第三方下载限制)

### 故障表现
* 下载模组包（Modpack）或单个模组时，部分文件下载失败并显示带有警告标识。
* 提示“限制第三方启动器下载”（Restricted third-party downloads）。

### 问题原因
CurseForge 平台上的部分模组作者关闭了第三方 API 的下载权限，强制要求玩家必须访问其官方网页来下载模组以获取广告收益。

### 解决方案
XMCL 智能集成了手动补全机制，您可以非常方便地补齐这些缺失的文件：
1. 在启动器右上角打开任务管理器，查看下载失败的详细任务。
2. 点击失败模组旁边的下载链接，这会在您的默认浏览器中打开该模组的 CurseForge 网页。
3. 在网页中手动将该 `.jar` 模组文件下载到您的本地电脑。
4. 将浏览器下载好的 `.jar` 文件直接 **拖拽（Drag-and-Drop）** 到 XMCL 启动器窗口中（或手动将其放入当前实例的 `mods` 文件夹中）。
5. XMCL 会自动检测并匹配该文件，随后自动继续并完成其余的安装流程。

---

## 🔄 3. 文件损坏一直重复下载 (控制校验和不匹配循环)

### 故障表现
* 启动器一直在重复下载某一个库文件或资源文件，并提示文件损坏。
* 游戏因为文件校验失败而一直无法正常启动。

### 问题原因
由于之前的某次下载被异常中断，损坏的缓存文件在本地被占用锁死，导致启动器无法用正确的文件覆盖它。

### 解决方案
1. 在启动器的诊断信息或日志中找到受损文件的具体路径（例如 `libraries/org/lwjgl/...`）。
2. 在实例面板中点击右上角的 **文件夹** 图标，打开当前实例的数据目录。
3. 按照错误提示中的路径找到对应受损库文件所在的**整个文件夹并将其彻底删除**。
4. 在启动器中点击 **修复**（Repair）或重新启动游戏。启动器此时会重新下载一个全新、完整的文件副本。

---

## ☕ 4. 游戏启动后瞬间崩溃或闪退 (Java 版本不兼容)

### 故障表现
* 点击启动后，游戏启动了但瞬间闪退，并返回错误代码 `1` 或 `-1`。
* 游戏日志（Log）中出现 `UnsupportedClassVersionError` 或“未找到 Java”等报错。

### 问题原因
不同版本的 Minecraft 对 Java（JDK）版本有着严格的要求。使用不匹配的 Java 版本会导致游戏直接崩溃。

### 解决方案
XMCL 拥有极其智能的自动 Java 管理器，可以替您一键下载并配置兼容的 JDK。

:::warning Java 版本兼容对照表
请确保您的实例配置了正确的 Java 版本：
* **Minecraft 1.12.2 及更早版本**：Java 8
* **Minecraft 1.16 - 1.17**：Java 16 / 17
* **Minecraft 1.18 - 1.20.4**：Java 17
* **Minecraft 1.20.5 及更高版本**：Java 21
:::

#### 如何在 XMCL 中管理和选择 Java：
1. 前往实例的设置页面（启动按钮旁边的齿轮图标）。
2. 找到 **Java** 设置区域。
3. 点击选择框。XMCL 会列出您系统上检测到的所有 Java 版本，并用绿色的勾号标记出完全兼容的版本。
4. 如果您的系统上没有合适的 Java 版本，请点击 **安装 Java**，启动器将自动下载并配置最适合您当前游戏版本的 JDK。

---

## 📑 5. 启动器本身无法打开或出现黑屏

### 故障表现
* 双击启动器图标后没有任何反应。
* 启动器窗口能打开，但整个界面是一片漆黑。

### 解决方案
您可以通过查看主日志文件来排查导致崩溃的具体原因：
1. 访问您本地的启动器数据文件夹：
   * **Windows**：在运行（Win+R）中输入 `%appdata%\xmcl` 并回车。
   * **macOS**：`~/Library/Application Support/xmcl`
   * **Linux**：`~/.config/xmcl`
2. 打开其中的 `logs` 文件夹，并找到最新的 `main.log` 文件。
3. 将此日志文件打包并提交给 Discord 社区的开发团队，或在 GitHub 上提交 Issue 寻求技术支持。
